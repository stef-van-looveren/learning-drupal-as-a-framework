<?php
use Drupal\views\ViewExecutable;

/**
 * Implements hook_views_pre_render().
 */
function notification_views_pre_render(ViewExecutable $view) {
  if (isset($view) && ($view->storage->id() == 'notifications')) {
    $view->element['#attached']['library'][] = 'notification/notification';
  }
}
